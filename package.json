{
  "name": "visioncraft-studio-preview",
  "private": true,
  "version": "2.0.0",
  "scripts": {
    "build": "python3 scripts/build.py && python3 scripts/check.py",
    "check": "python3 scripts/check.py"
  },
  "engines": {
    "node": ">=20"
  }
}
